{
  "name": "flashcard-server",
  "version": "1.0.0",
  "description": "Backend for FlashCard Study App",
  "main": "server/services/server.js",
  "type": "module",
  "scripts": {
    "start": "node server/services/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
