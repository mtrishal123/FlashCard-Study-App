{
  "name": "flashcard-fullstack",
  "version": "1.0.0",
  "description": "Fullstack FlashCard App (Express + React)",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2"
  }
}
